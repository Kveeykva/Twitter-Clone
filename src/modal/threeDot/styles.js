import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  threeDot: {
    fontSize: 12,
  },
  threeDotView: {justifyContent: 'center', alignItems: 'flex-end', flex: 1},
});

export default styles;
